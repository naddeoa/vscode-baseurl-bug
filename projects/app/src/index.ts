// This will show as an error in vscode if vscode was oppend from the repo root.
// It won't show as an error if vscode was opened from the `projects/app` project.
import { something } from "something";

console.log(something);
